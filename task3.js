function power(x,y)
{
    return Math.pow(x,y);
}

module.exports.power = power;

function round(x){
    return Math.round(x);
}

module.exports.round = round;

function ceiling(x){
    return Math.ceil(x);
}

module.exports.ceiling = ceiling;